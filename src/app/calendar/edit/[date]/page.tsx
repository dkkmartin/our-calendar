import NewEvent from "@/components/calendar/newEvent"

interface CalendarEditProps {
  params: {
    date: string
  }
}

export default function CalendarEdit({ params }: CalendarEditProps) {
  return <section></section>
}
